# Homebrew Tap for tenseleyFlow

Homebrew formulae for tenseleyFlow CLI tools and utilities.

## Installation

```bash
brew tap tenseleyFlow/tap
```

## Available Formulas

| Formula | Description | Install |
|---------|-------------|---------|
| **fackr** | Terminal text editor written in Rust (facsimile reimplementation) | `brew install fackr` |
| **fussr** | Git staging TUI tool - Rust port of fuss | `brew install fussr` |
| **gitswitch** | Secure Git identity and SSH/GPG key management | `brew install gitswitch` |
| **hyprkvm** | Hyprland-native software KVM switch | `brew install hyprkvm` |
| **parrot** | Intelligent CLI command failure assistant with AI-powered responses | `brew install parrot` |

## Quick Install Examples

```bash
# Install the tap first
brew tap tenseleyFlow/tap

# Then install any formula
brew install gitswitch
brew install hyprkvm
brew install parrot
brew install fackr
brew install fussr
```

## Links

- [tenseleyFlow Organization](https://github.com/tenseleyFlow)
